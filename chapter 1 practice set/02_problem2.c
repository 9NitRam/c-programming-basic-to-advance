#include <stdio.h>
   int main (){
     int r = 6;
     int h =8;
     printf("the area of the cicle is %d is %f\n",r, 3.14*r*r);
     printf("the volume of cyclinder is r is %d  and h is %d is %f\n",r,h, 3.14*r*r*h );
     return 0;
   }